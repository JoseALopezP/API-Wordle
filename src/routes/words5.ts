import express from 'express'

const router = express.Router()

router.get('/', (_req, res) => {
    //send random word
})

router.post('/', (_req, res) => {
    //check for word and send correct words
})